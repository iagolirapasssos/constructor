document.addEventListener("DOMContentLoaded", function() {
    initializeApp();
});

function initializeApp() {
    initializeUI();
    initializeGame();
    initializeEvents();
    initializeActions();
}
